spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/here
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: rudqnreo16!

server:
  servlet:
    context-path: /api

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.dto
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

logging:
  level:
    com.here.memo.dao: DEBUG
    com.here.user.dao: DEBUG
    org.mybatis: DEBUG
    jdbc.sqlonly: DEBUG